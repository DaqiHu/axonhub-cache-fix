display_name: Extension Dev
short_description: Build traced, semantic-preserving cache-fix extensions
default_prompt: Use $extension-dev after proving a cache-breaking prefix mutation, then add a trace-derived test and deploy it safely

display_name: Cache Hit Debug
short_description: Diagnose root causes of low DeepSeek cache hit rates using logs and request traces
default_prompt: Use $cache-hit-debug to trace the root cause of a cache miss and identify which extension handles it
